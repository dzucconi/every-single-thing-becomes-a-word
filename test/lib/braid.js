import braid, { zip } from '../../lib/braid';

describe('zip', () => {
  // console.log(zip([['a', 'b', 'c'], ['x', 'y', 'z']]));
});

describe('braid', () => {
  it('braids the text', () => {
    // console.log(braid('foo bar baz'));
  });
});
